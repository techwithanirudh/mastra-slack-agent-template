--- a/dist/chunk-OE4IEL7C.js
+++ b/dist/chunk-OE4IEL7C.js
@@ -2703,12 +2703,28 @@
               if (invocation.result !== void 0) {
                 if (typeof invocation.result === "string") {
                   tokenString += invocation.result;
+                } else if (invocation.result && typeof invocation.result === "object" && typeof invocation.result.data === "string" && typeof invocation.result.mediaType === "string") {
+                  const mediaType = invocation.result.mediaType;
+                  const dataLength = invocation.result.data.length;
+                  const estimatedBytes = Math.floor(dataLength * 3 / 4);
+                  const bySize = Math.round(estimatedBytes / 750);
+                  const fallback = mediaType.startsWith("image/") ? 765 : 258;
+                  overhead += bySize > 0 ? Math.max(200, Math.min(4000, bySize)) : fallback;
                 } else {
                   tokenString += JSON.stringify(invocation.result);
                   overhead -= 12;
                 }
               }
             }
+          } else if (part.type === "file") {
+            const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+            tokenString += `${part.filename ?? ""}${mediaType}`;
+            const url = typeof part.url === "string" ? part.url : "";
+            const base64Length = url.includes(",") ? url.length - url.indexOf(",") - 1 : url.length;
+            const estimatedBytes = Math.floor(base64Length * 3 / 4);
+            const bySize = Math.round(estimatedBytes / 750);
+            const fallback = mediaType.startsWith("image/") ? 765 : 258;
+            overhead += bySize > 0 ? Math.max(200, Math.min(4000, bySize)) : fallback;
           } else {
             tokenString += JSON.stringify(part);
           }
--- a/dist/chunk-CJAAPSS7.cjs
+++ b/dist/chunk-CJAAPSS7.cjs
@@ -2734,12 +2734,28 @@
               if (invocation.result !== void 0) {
                 if (typeof invocation.result === "string") {
                   tokenString += invocation.result;
+                } else if (invocation.result && typeof invocation.result === "object" && typeof invocation.result.data === "string" && typeof invocation.result.mediaType === "string") {
+                  const mediaType = invocation.result.mediaType;
+                  const dataLength = invocation.result.data.length;
+                  const estimatedBytes = Math.floor(dataLength * 3 / 4);
+                  const bySize = Math.round(estimatedBytes / 750);
+                  const fallback = mediaType.startsWith("image/") ? 765 : 258;
+                  overhead += bySize > 0 ? Math.max(200, Math.min(4000, bySize)) : fallback;
                 } else {
                   tokenString += JSON.stringify(invocation.result);
                   overhead -= 12;
                 }
               }
             }
+          } else if (part.type === "file") {
+            const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+            tokenString += `${part.filename ?? ""}${mediaType}`;
+            const url = typeof part.url === "string" ? part.url : "";
+            const base64Length = url.includes(",") ? url.length - url.indexOf(",") - 1 : url.length;
+            const estimatedBytes = Math.floor(base64Length * 3 / 4);
+            const bySize = Math.round(estimatedBytes / 750);
+            const fallback = mediaType.startsWith("image/") ? 765 : 258;
+            overhead += bySize > 0 ? Math.max(200, Math.min(4000, bySize)) : fallback;
           } else {
             tokenString += JSON.stringify(part);
           }
