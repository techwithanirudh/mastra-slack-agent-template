# gorkie TODO

Source of truth for outstanding work. Grouped by area. See [DESIGN.md](./DESIGN.md) for architecture.

##
- Refactor back to agent view
- Make mastra dev and prod projects, use Mastra's DB offering rather than Supabase. Also, add devarsh
- Add ability for Gorkie to render generative UI
- Add proper docs for usage with Fumadocs
  E.g: How to add new MCPs
  How to disable Tool Display
- Remove restrictions from post_message, upload_file, etc. etc. for template
- Cleanup skills
- Add DOCS on configuring mastra observability, deployment etc.
- See if we deploy gorkie on mastra?
- The assistant panel passes channel id and thread id

## Priority queue

- [ ] **DM tool display — root-caused (2026-07-07), third fix attempt shipped, needs live verification**: confirmed via two live screenshots that DMs and scheduled/heartbeat-fired tasks both get stuck on a bare `...` placeholder forever instead of rendering tool cards. Root cause, fully traced through the vendored `chat`/`@chat-adapter/slack` packages: `chat`'s `handleStream()` (`node_modules/chat/dist/index.js:1517`) only uses the native live Slack streaming widget (which is what renders tool-call cards) when the adapter's `stream()` returns a message; otherwise it falls back to `fallbackStream()` (`index.js:1609`), a **text-only** post+edit loop with zero awareness of tool calls, whose placeholder is `"..."` by default (`chat` package default for `fallbackStreamingPlaceholderText`) — this never gets replaced if the model's final text stays empty, which is exactly the earlier "empty message" bug too. `@chat-adapter/slack`'s `stream()` (`dist/index.js:3556`) returns `null` (forcing that fallback) whenever `threadTs` is falsy. For a DM's top-level/outer conversation, the adapter's own dispatcher deliberately encodes `threadTs` as `""` when there's no explicit Slack thread (`dist/index.js:2132`: `isDM ? event.thread_ts || "" : event.thread_ts || event.ts` — DMs get `""`, channels fall back to `event.ts`), which is intentional (keeps a whole DM as one continuous conversation rather than fragmenting per top-level message, unlike channels) but also means `stream()` always sees a falsy `threadTs` for that case. Slack's real `chat.startStream` API requires a non-empty `thread_ts` (confirmed via `@slack/web-api`'s `ChatStartStreamArguments extends ... ThreadTS`, not `Partial<ThreadTS>`) — there is no way to get live tool-call rendering in a DM without Slack anchoring to some thread; the text-only fallback path is structurally incapable of rendering tool calls regardless of any patch.
  - **First fix attempt, reverted same day**: patched `@chat-adapter/slack` directly to resolve `thread_ts` via `conversations.history` on *every* call when empty. Wrong: re-resolved "most recent message" every time, so after the bot's own reply landed, the next turn anchored to a different message, fragmenting one flat DM into scattered one-off threads. Also only fixed the streamed reply — the completion footer (`processors/turns.ts:139`) posted through a separate `slack.postMessage()` call untouched by the patch, so it kept landing outside the thread. Fully reverted (confirmed via clean `bun install`, no trace, `typecheck`/`check` pass).
  - **Second fix attempt, also reverted same day**: instead of a vendored patch, overrode `postMessage()` and `stream()` on gorkie's own `GorkieSlackAdapter` subclass (`src/mastra/chat/adapter.ts`, same pattern already used there for `resolveInlineMentions`) with a shared `anchorDmThreadId()` helper — for a DM with no explicit thread, check `this.chat.getState()` (the same Postgres-backed state channels already uses) for a persisted `dm-thread-anchor:{channel}` key, reuse it if present, otherwise resolve the current most-recent message via `conversations.history` once and persist it. This avoided the first attempt's fragmentation (one stable anchor reused forever) and fixed the footer too (every call path shares the same adapter instance). Typechecked and linted clean. **Reverted anyway, before live-testing**, once a real remaining risk surfaced: this only changes the *outgoing* post/stream calls, not the *incoming* dispatch logic that computes the external thread id Mastra uses for memory continuity (still `dist/index.js:2132`, untouched). Under normal DM use (typing in the main composer) that's fine — the user's messages never carry `thread_ts`, so the external id stays the stable `""` regardless of what the bot's replies are anchored to. But once the bot's first reply creates a real visible Slack thread, a user *could* explicitly click into it and reply there, which *would* carry `thread_ts` equal to the anchor — and dispatch would then compute a *different* external id for that message (`channel:<anchorTs>` instead of `channel:""`), causing Mastra to treat it as a different conversation and fragment memory for that reply. Also still unverified: `chat/events.ts` already wires up `onAssistantThreadStarted`/`onAssistantContextChanged`, meaning Slack's AI-Apps Assistant sidebar is in play for this bot and that flow hands back a real `thread_ts` from the start — if DMs are normally used through that sidebar rather than a classic 1:1 chat, the flat `""` case (and this whole fix) might rarely even engage. Needed more certainty on both points before shipping a change to live DM behavior; not established at the time, so reverted (`git restore` back to the original `HEAD` content, confirmed via `git diff HEAD` clean).
  - **Third fix attempt, shipped (2026-07-07)**: confirmed live (via E2E testing in Slack) that gorkie's DMs go through the Assistant sidebar, not classic 1:1 chat — that changes the risk profile from the second attempt, since the Assistant panel doesn't expose a "reply in thread" affordance the way a normal message does, so the memory-fragmentation edge case that blocked the second attempt is much less reachable in practice. Also, `assistant_thread_started` (already wired up in `chat/events.ts`) hands back a *real* `thread_ts` from Slack itself the moment a new Assistant conversation starts — a much better anchor source than the second attempt's `conversations.history` heuristic. New design: `chat/dm-anchor.ts` centralizes the shared state key (`dm-thread-anchor:{channel}`) so both sides agree. `chat/events.ts`'s `onAssistantThreadStarted` handler now proactively persists `event.threadTs` as the anchor (via `anchorNewAssistantThread`) the moment Slack tells us a conversation started — this is the common-case path, no guessing needed. `chat/adapter.ts`'s `anchorDmThreadId()` (same `postMessage()`/`stream()` override shape as the second attempt) is now just the fallback for anything that reaches the adapter before that event fires: check the persisted anchor first, only fall back to the `conversations.history` heuristic if nothing's persisted yet. This also fixes a second bug found live in this session: `post_message` targeting a user (`target.type === 'user'` → `chat().openDM()`) resolves to the same stable-but-unanchored DM identity every call, so repeated posts to the same person were landing as separate unthreaded messages — Slack's Assistant sidebar showed 5 test DMs as 5 separate conversations instead of one. The adapter override fixes this the same way, since `post_message` ultimately calls the same `postMessage()` method. **Bonus feature**: also wired up `setAssistantTitle` (already existed as an unused adapter method, same family as `setSuggestedPrompts`) to set a default conversation title when a new Assistant thread starts, answering an open question about parity with an older version of this bot. Typechecked and linted clean (`bun run typecheck`, `bun run check`). **Not yet live-tested** — needs a real fresh DM (first message, to confirm the Assistant sidebar shows one conversation and tool cards render) and a `post_message`-to-user smoke test (multiple sends landing in one thread) before closing this out.
  - **`post_message` refined further (2026-07-07)**: its own `postMessage()` override no longer falls back to the `conversations.history` guess when there's no anchor yet — since it can post unanchored (becoming its own thread root, same as a channel's first message), it does that directly, persists the result as the anchor, and corrects the returned `threadId` immediately instead of the caller only picking up the new anchor on its *next* call. `stream()` still needs the `conversations.history` fallback since Slack's streaming API requires `thread_ts` up front.
  - **AI-generated conversation titles (2026-07-07)**: the static `"gorkie"` title set at `assistant_thread_started` is now just a placeholder — `processors/turns.ts` generates a real one after the first completed DM turn, using `summarizerAgent` (same cheap model already used for thread summaries) on the user's first message + gorkie's reply, then calls `setAssistantTitle` and marks it done (new `dm-thread-titled:{channel}` flag in `chat/dm-anchor.ts`) so it only fires once per conversation. Fires in the background (not awaited), so it can't add latency to the actual reply. Deliberately reads the anchor from persisted state rather than the dispatch-side `threadTs` (which stays `""` for a classic flat DM even after the reply anchors it, since dispatch itself is untouched) — this was caught and fixed before shipping, since the naive version would have silently never fired outside the Assistant-sidebar case.
  - **Known tradeoff, discussed and accepted**: `chat/handlers.ts`'s `onSubscribedMessage` fix (below) forces a fresh Mastra thread-history backfill on every mid-thread mention gorkie isn't actively following, with no deduping against what's already in gorkie's own conversational memory — confirmed there's no deduping at that layer (`fetchThreadHistory` in `@mastra/core` just grabs the last N raw messages fresh every call). Back-to-back re-mentions will re-send some already-seen content. Accepted as the right tradeoff (redundant-but-complete beats cheap-but-missing-context); a tighter fix (track the last message gorkie actually responded to, skip the forced refetch when there's provably no gap) is possible later if the token cost becomes a real problem.
  - **Still separately open**: the scheduled/heartbeat-task case is a *different* trigger of the same fallback path (see the standalone item below, promoted out of this sub-bullet since it's a distinct bug in its own right, not just a footnote on the DM fix).
- [ ] **P0: Scheduled/heartbeat-fired tasks don't render tool cards either — same fallback path as the DM bug, different trigger, still unfixed**: when a scheduled task fires, its run also falls back to `chat`'s text-only `fallbackStream()` (see the DM tool display item above for what that path can't do — no tool-call rendering, can get stuck on a bare `"..."` placeholder). Root cause here is different from the DM case: `chat`'s `handleStream` only populates `recipientUserId`/`recipientTeamId` (required by Slack's native streaming API for non-DM channels) from a live inbound message event (`this._currentMessage`), which doesn't exist for a heartbeat-woken run — there's no originating webhook event, just an internal fire. The DM anchor fix (above) doesn't touch this at all, since it's a completely different reason for hitting the same fallback. Fixing this needs `recipientUserId`/`recipientTeamId` — we already know the task creator from `metadata.createdBy` in `tools/scheduled-tasks/create.ts` — plumbed through `@mastra/core`'s heartbeat/schedule `streamOptions`, which is currently undocumented/unclear whether it's even exposed for that purpose. Needs its own investigation before a fix is possible.
- [x] **`observability.duckdb` disk-space risk — resolved by dropping local observability storage entirely (2026-07-07)**: found the host disk at 94% usage (~34 GB free) with the DuckDB file alone at ~12 GB and no retention/pruning (every trace/span write accumulated forever; confirmed `@mastra/duckdb` doesn't implement the new `@mastra/core@1.50.0` storage-retention/`prune()` feature — only Postgres/libSQL/MongoDB adapters do). Considered migrating to `PostgresStoreVNext` (Postgres now has first-class retention support) but decided against building *any* local observability domain: `MastraPlatformExporter` already ships every trace to Mastra Platform (cloud), so local storage was only ever serving two things — the completion footer's `$` cost line and ad-hoc local SQL debugging — and both are outweighed by the complexity of standing up a second store just for that. **Final architecture**: `src/mastra/index.ts` now uses a single plain `PostgresStore` for all storage (memory, channel state, no domains split, no `MastraCompositeStore`), and `Observability` only has `MastraPlatformExporter` configured (`MastraStorageExporter` removed, since there's no local observability domain left for it to write into — it would fail without one). Removed the `@mastra/duckdb` dependency entirely. **Known, accepted tradeoff**: `processors/turns.ts`'s completion footer no longer shows the `$0.0023 USD` cost line (that came from a local `getMetricAggregate` query); token counts and tok/s are unaffected since those come straight from `usage`, no DB involved. The old 12 GB `observability.duckdb` (+ `.wal`) file was fully orphaned, and has now been deleted (confirmed): disk usage dropped from 94% to 90% (34 GB → 49 GB free).
- [ ] **Later: Mastra Studio running-agent experience**: improve how a running agent looks/behaves in Studio (progress, streaming, cancellation).
- [ ] **P0: Upstream the `@mastra/core` file-part token-estimation patch**: `patches/@mastra+core@1.50.0-alpha.2.patch` fixes token estimation for `file`-type message parts (currently `JSON.stringify`'d into the token string, wildly overcounting/undercounting for base64 file/image content). This has already broken three times now from alpha version bumps changing internal chunk bundling/hashing (`1.50.0-alpha.0` → `alpha.1` → `alpha.2`, each requiring manually locating the new chunk files and regenerating the diff from scratch — see `scripts/verify-patches.ts`, the postinstall safety net that catches this). The byte-size-scaled calculation (base64 length → estimated bytes → clamped token estimate, not just a flat per-part guess) also got dropped once during an alpha.2→alpha.1 downgrade-and-back and had to be manually reapplied (2026-07-05) — the patch file and live `node_modules` had drifted out of sync with each other. **A second, previously-unpatched instance of the exact same class of bug was found and fixed (2026-07-05)**: `read_file` on an image still triggered "No messages fit within the remaining token budget" even with the file-part patch correctly applied and verified. Root cause: Mastra's built-in `read_file` tool returns a plain JS object for media files — `{ data: <base64>, mediaType, ... }` (confirmed in `workspace/tools/read-file.ts`) — as the tool's literal invocation *result*, not as a message-content `file` part. `countInputMessageTokens` has a **separate** code path for tool-invocation results (`invocation.result`) that also falls back to `JSON.stringify(invocation.result)` for any non-string result, independent of the message-content-parts path the original patch fixed. Applied the same byte-size-scaled-estimate technique to this second branch. Both fixes now ship together in the same `alpha.2` patch. File an upstream PR against mastra-ai/mastra with both fixes (same pattern as the already-merged [PR#18396](https://github.com/mastra-ai/mastra/pull/18396)) so we can drop the patch entirely.
- [x] **DM-relay approval gap — fixed with a self-only policy (2026-07-07)**: re-verified against the current code and the *read* side was already fully blocked — `get_channel_info`, `list_threads`, `read_conversation_history`, and `summarize_thread` all route through `assertReadableChannel` (`tools/slack/utils.ts`), which only allows the current thread or `channelVisibility === 'workspace'` (public). `search_slack` is separately safe too — it's scoped to the requesting user's own Slack `action_token`. The real gap was on the **write** side: `assertCanPostTo` had `if (isCurrentThread || target.type === 'user') { return; }` — a `post_message`/`upload_file` call targeting a *user* (DMing a third party on the requester's behalf) had **zero** checks. Fixed alongside the same-channel-only channel restriction (above): a user target must now be the requesting user themselves — `rawId(target.id) !== rawId(requestingUserId)` throws. Mirrors how `schedule_reminder` already only ever DMs the current user. DMing a third party on someone else's behalf is no longer possible at all. Typechecked and linted clean. **Not yet live-tested.**
- [x] **`post_message`-to-channel restriction gap — fixed with a same-channel-only policy (2026-07-07)**: earlier in this project, live-observed that gorkie could relay a message into a restricted/announcement-only channel (e.g. `#macondo-bulletin`) where regular members can read but not post, even though a human member couldn't type there themselves. Root cause: `assertCanPostTo` (`tools/slack/utils.ts`) only checked *membership* for channel targets, not *posting rights* — and Slack's real "who can post" API (`admin.conversations.getConversationPrefs`) is Enterprise-Grid-only and needs a **user** token gorkie doesn't have, so verifying posting rights directly isn't feasible. Brainstormed several options (allowlist+admin-gate, confirm-once flow, public-only tightening, audit trail, rate limiting); decided on the simplest and most restrictive: **`post_message`/`upload_file` can now only target the channel the current conversation is already in** (any thread within it is fine; a genuinely different channel is refused outright, no exceptions, no admin override). Removed the old `isChannelMember` membership check (`conversations.members` call) entirely — same-channel is now a plain synchronous comparison (`chatChannelId(targetChannelId) === chatChannelId(currentChannelId)`), so `assertCanPostTo` is no longer even `async`. `target.type === 'user'` (DMing a third party) is intentionally untouched by this — that's the separate, still-open DM-relay gap below. Updated the `post_message` tools-prompt note accordingly. Typechecked and linted clean. **Not yet live-tested.**
- [ ] **P1: Signals and email monitor**: add event triggers such as "email arrived, spawn an agent", process/respond automatically only when appropriate, and wire an email monitor.
- [ ] **P1: GitHub repo troubleshooting workflow**: add GitHub account access, repo cloning, code inspection, and troubleshooting flows for user repos.
- [ ] **P1: `upload_file` custom emoji upload — real platform blocker found, needs an owner decision**: `get_file` can already download a custom emoji by shortcode (done 2026-07-04, needs `emoji:read` scope added + app reinstall). Upload is a different story: Slack's only programmatic path (`admin.emoji.add`) requires an Enterprise Grid org, an org-admin token, and a *publicly reachable URL* (Slack fetches the image itself; you can't POST bytes). None of that fits gorkie's setup. The only known working alternative uses Slack's undocumented internal `https://{team}.slack.com/api/emoji.add` endpoint, authenticated with a real user's browser session cookie + xoxc token (not a bot token) — i.e. a human would hand gorkie a full account-impersonation credential, not a scoped one. Flagged this to the user as a materially different risk tier (personal-account compromise if it leaks, ToS/ban risk, no API stability guarantee) rather than building it unprompted. Waiting on an explicit go-ahead from @twa or @Devarsh before wiring in session-cookie credentials.
- [ ] **Later: Tool display preference**: let users choose hidden vs original/visible tool display mode.
- [ ] **Later: `upload_file` target support**: add upload support for thread/channel/DM targets similar to `post_message`.

## Open questions (need your call)

- [ ] **`resourceId` for shared threads**: channels defaults to the first speaker's `slack:<userId>`. Key on the channel/thread instead (so memory belongs to the conversation, not one person)? Ties to OM scope (thread vs resource).

## Recently completed

- **Cleaned up Slack message-management tools (2026-07-08)**: removed one-line helper functions for current-thread/self-DM checks and inlined those checks at the call sites. Kept edit/delete Slack message source parsing in `tools/slack/utils.ts`, kept ownership checks in `tools/slack/utils.ts`, and let `recordPostedMessage` derive the recorded Slack channel from the posted target/result.
- **Inlined Slack edit/delete input schemas into tool definitions (2026-07-08)**: removed the remaining `editMessageInputSchema` and `deleteMessageInputSchema` constants. `edit_message` and `delete_message` now define their schemas directly on `inputSchema`.
- **Inlined Slack edit/delete execute helpers (2026-07-08)**: removed the one-off `editMessage` execute wrapper and removed the shared `resolveMessageSource` branch helper. `edit_message` and `delete_message` now parse url/id inputs directly in each tool execute body.
- **Inlined Slack edit/delete message source schemas (2026-07-08)**: removed the schema-only `src/mastra/types/tools/slack-message.ts` file. `edit_message` and `delete_message` now keep their small `z.discriminatedUnion('source', ...)` schemas directly in each tool.
- **Documented dev/prod Mastra Platform observability (2026-07-08)**: README now explains using two Mastra Platform projects with the same env var names and different per-environment values. `.env.example` calls out dev vs prod project ids. `DESIGN.md` now matches current code: Platform exporter only, no DuckDB or storage exporter.
- **Restored selected prompt tool notes (2026-07-08)**: kept the helper-agent note, restored the `execute_command` sandbox-lifetime note, and restored the normal file-tool preference note. The file-tool note now excludes `grep` and no longer carries the grep-specific performance guidance.
- **Removed obvious tool prompt notes (2026-07-08)**: trimmed `src/mastra/prompts/tools.ts` again so obvious file-editing, listing, grep, and shell-command usage rely on tool definitions instead of extra prompt text. Kept non-obvious policy and safety notes for delegation, Slack ids, message management, turn control, recurring tasks, and `read_file` binary/media hazards.
- **Root-caused the `BUG.md` sandbox/browser timeout (2026-07-08)**: read Mastra Platform trace `7392d4f7e41c37de2049214d07fa8d57` for Slack thread `C0A6C5F52BE/p1783487078724849`. The timeout was not a general sandbox background-process failure. The first Next dev server printed `Ready`, but its `next-server` process wedged in `D` state and `curl localhost:3000` hung. After killing the stuck Next and agent-browser processes, a fresh `PORT=3000 npx next dev -p 3000` served HTML successfully. `BUG.md` now records the diagnosis and verification notes.
- **Finished the prompt tool cleanup (2026-07-08)**: removed the remaining non-note prompt context from `src/mastra/prompts/tools.ts`, including the duplicate helper-agent description and broad lookup block. The file now keeps only note-style behavioral guidance, with actual tool context left to tool definitions.
- **Tightened `scripts/delete-slack-messages.ts` again (2026-07-08)**: trimmed the one-use token helper, normalized prompted and positional URLs, prints deletion results in input order, and preserved the explicit `--token` or pasted-token flow with no direct env reads.
- **Removed Mermaid and refreshed README/task docs (2026-07-08)**: removed the `mermaid` tool from the registered base tools and deleted its implementation, trimmed the tools prompt so durable tool-use guidance lives on individual tool descriptions, updated README to reflect current Postgres, Mastra Platform observability, AI image, and mid-thread backfill behavior, and cleaned up `BUG.md` wording.
- **Deleted the two linked production Gorkie Slack messages and added same-requester message management (2026-07-08)**: initial deletion with the dev token failed with Slack `cant_delete_message`; retrying with an explicit production bot token successfully deleted both linked messages. Added `scripts/delete-slack-messages.ts`, which accepts `--token=...` or prompts interactively for a pasted token. Added `edit_message` and `delete_message` tools. `post_message` now records each sent message's Slack channel/timestamp, original requester, and self-DM status in chat state; edit/delete only work for messages recorded this way and only when the current Slack user is the same requester. Follow-up cleanup removed the extra shared target type layer, inlined the tool input schemas, kept `process.env` access inside `src/env.ts`, and left `utils.ts` checking only the normalized `{ channel, ts }` identity.
- **Cleaned up `delete-slack-messages` CLI (2026-07-08)**: rewrote argument handling around `Bun.argv` plus `node:util` `parseArgs`, added `--token xoxb-...` and `--token=xoxb-...` support, and made missing token/URL input interactive instead of immediately throwing usage. Removed the fake `SLACK_BOT_TOKEN_PROD` env path and `slackMaintenanceEnv`; maintenance deletion now requires an explicit pasted/input token.
- **`post_message`'s DM anchor now mints from its own first post instead of guessing (2026-07-07)**: refined the third DM-anchor fix attempt (see the "DM tool display" item above). `postMessage`'s override in `chat/adapter.ts` no longer falls back to a `conversations.history` guess when there's no anchor yet — since `postMessage` can post unanchored (a fresh top-level message becomes its own thread root, same as a channel's first message), it just does that, persists the resulting message as the anchor, and corrects the returned `threadId` to reflect it immediately (the base adapter otherwise echoes the caller's original, unanchored threadId back unchanged). `stream()` still needs the `conversations.history` fallback since Slack's streaming API requires a `thread_ts` up front and can't post unanchored the way a plain message can.
- **Mid-thread re-mentions were skipping the thread-history backfill — root-caused and fixed (2026-07-07)**: live-reported bug — pinging gorkie a second time in the middle of a thread (not the thread root) replies but doesn't re-fetch the last 10 messages, even though gorkie never saw what happened between the two mentions. Root cause, traced into `@mastra/core`: `processChatMessage` (`chunk-WNAR4CR3.js:12180`) only backfills thread history when `!chatThread.isSubscribed()` (`:12196-12198`), but calls `chatThread.subscribe()` unconditionally on *every* processed message (`:12316`) regardless of which handler ran. Gorkie's own `respondOnThreadMessages` flag (`chat/handlers.ts`) is a completely separate, gorkie-owned concept that only gets set for root mentions — so a one-off mid-thread mention leaves Mastra's *own* subscription flag `true` from that first exchange onward, silently skipping backfill on every mention after the first, even when gorkie isn't actually following the thread in between. Fixed in `chat/handlers.ts`'s `onSubscribedMessage`: when responding to a mention we're not actively following (`respondOnThreadMessages` not set), call the (public) `thread.unsubscribe()` right before handing off to `defaultHandler`, forcing Mastra to see an unsubscribed thread and re-fetch history for that turn; Mastra re-subscribes on its own immediately after. `onMention` doesn't need the same fix — per `chat`'s own docs it's only ever called for mentions in threads Mastra already considers unsubscribed, so backfill already fires correctly there.
- **Subagent stability and delegate long-run failure state — confirmed fixed (2026-07-07)**: both former P0s (subagents broken in Slack/interruption behavior, and long helper-agent runs showing a generic parent error) are resolved per direct confirmation; no longer tracked as open.
- **Migrated `@mastra/core` off the alpha channel to stable `1.50.0` (2026-07-07)**: was pinned to `1.50.0-alpha.2` solely to get ahead of unreleased fixes our token-estimation patch needed. Checked the actual diff between the newest alpha (`1.50.1-alpha.0`) and stable `1.50.0` first — it's one unrelated commit (provider/model registry data refresh), so there was no reason to stay on the alpha treadmill that had already broken our patch three times. Also bumped `@mastra/observability` (`1.16.0-alpha.0` → `1.16.0` stable), `@mastra/pg` (`1.15.0` → `1.15.1`), `@mastra/e2b` (`0.5.0` → `0.5.1`), and the `mastra` CLI devDependency (`1.18.0` → `1.18.1`). Regenerated the token-estimation patch against the new chunk files (`chunk-JGDMZZAO.js`/`chunk-EVJSSG7F.cjs`, replacing `chunk-WNAR4CR3.js`/`chunk-EMMWR65D.cjs`) using the same manual tarball-diff process as before (`bun patch`'s own diffing is broken in this sandbox — `.gitattributes` symlink resolution error), updated `scripts/verify-patches.ts` to check the new filenames, and added the new chunk hash substrings to `.cspell.jsonc`. **Found and migrated a real breaking change along the way**: `mastra.heartbeats` (the whole `Heartbeat`/`Heartbeats` API from `@mastra/core/agent`) was renamed to `mastra.schedules` (`AgentSchedule`/`Schedules` from the new `@mastra/core/schedules` subpath) somewhere between our old alpha and this stable release — same field shapes (`agentId`, `cron`, `prompt`, `threadId`, `resourceId`, `tagName`, `ifActive`, `ifIdle`, `metadata`, etc., confirmed via the new `.d.ts`), but `list()`/`pause()`/`resume()` now return the wider `AnySchedule` union (agent or workflow schedules) instead of a narrow agent-only type, so `tools/scheduled-tasks/{queries,list}.ts` needed type-guard narrowing (`item.agentId !== undefined`) to filter to agent schedules before treating them as `AgentSchedule`. All six `scheduled-tasks/*.ts` files updated (`create`, `list`, `pause`, `resume`, `delete`, `queries`, `utils`); no backward-compat shim exists for `mastra.heartbeats` (fully removed). Verified with a clean `bun install` (patch reapplies from scratch) plus `typecheck`/`check`/`check:spelling`, all pass.
- **Migrated observability from Langfuse to Mastra Platform (2026-07-07)**: swapped `LangfuseExporter` for `MastraPlatformExporter` in `src/mastra/index.ts`, replaced `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`/`LANGFUSE_BASEURL` with `MASTRA_PLATFORM_ACCESS_TOKEN`/`MASTRA_PROJECT_ID` in `env.ts`/`.env.example`, removed the now-unused `@mastra/langfuse` dependency, and updated `README.md`/`DESIGN.md`/`AGENTS.md` accordingly. The local `MastraStorageExporter` → DuckDB path is unrelated and was deliberately kept (see the disk-space item below) — it's not a Langfuse-vs-Platform choice, it's local in-process query access (the cost/tok-s completion footer, ad-hoc SQL debugging) vs. a hosted dashboard, and both exporters run side by side.
- **Fixed concurrent delegate tool display (2026-07-05)**: delegated child tool-call IDs now carry their parent delegate `toolCallId`, so inner tool updates target the correct parent card even when two same-type agents run concurrently.
- **Killed running bot processes (2026-07-05)**: stopped the active `bun run dev`, `mastra dev`, and `.mastra/output/index.mjs` processes for this checkout.
- **Cleaned up tool display code (2026-07-05)**: split `tool-display` into `index.ts`, `agents.ts`, and `format.ts`, keeping the main dispatch small and moving delegate display plus formatting into focused modules.
- **Fixed missing tool input rendering (2026-07-05)**: tool display now falls back to Mastra's `argsSummary` when structured args are absent or empty, so inputs still render for those tool events.
- **Fixed skip-only footer bug (2026-07-05)**: `skip` no longer counts as a visible tool call for the completion footer, so a turn that only calls `skip` stays silent.
- **`chat` package version drift between top-level and `@mastra/core`'s bundled copy — fixed via `overrides` (2026-07-05)**: `@mastra/core` depends on `chat@^4.29.0` as a regular (non-peer) dependency, while gorkie's own `package.json` pins `chat@^4.32.0` at the top level. Since it's not a peer dependency, bun correctly resolved two separate copies — but they'd diverged enough (a new private field, a new `setMentionFlags` method) that TypeScript treated gorkie's own `Thread`/`Message` types as incompatible with what `@mastra/core`'s `ChannelHandler` expects, breaking `bun run typecheck`. Fixed by adding `"overrides": { "chat": "^4.32.0" }` to `package.json`, forcing a single deduped `chat` copy across the whole tree (confirmed: `node_modules/@mastra/core/node_modules/chat` no longer exists after reinstall). This is also suspected to be why DM markdown rendering, previously broken, now works correctly after this fix — not confirmed root-caused, just observed immediately after. Worth re-checking after any future `@mastra/core` bump whether its own `chat` dependency range has moved to `^4.32.0` or later, at which point this override can likely be dropped.
- **Fixed a real full-process crash from a dropped Slack Socket Mode connection (2026-07-05)**: live-observed in production logs — a WebSocket pong timeout ("A pong wasn't received from the server before the timeout of 5000ms!") dropped the connection, and the next queued event's `ack()` call then threw `Failed to send a WebSocket message as the client is not ready` from inside `@chat-adapter/slack`'s `startSocketMode()` event handler (`node_modules/@chat-adapter/slack/dist/index.js`, the `this.socketClient.on('slack_event', async (...) => {...})` callback), which has no try/catch around it. Since the callback is `async` and nothing awaits/handles its rejection, this became an unhandled promise rejection — fatal by default in Node — killing the entire bot process over one dropped socket message, not just that one event. Added a process-level safety net in `src/mastra/index.ts` (`process.on('unhandledRejection', ...)` / `process.on('uncaughtException', ...)`, logging instead of crashing) as a broad fix that also covers any other unhandled rejection in the dependency graph, not just this specific one. A more surgical fix (patching `@chat-adapter/slack` itself to wrap the handler in try/catch, same pattern as the existing `@mastra/core` patches) is still open if this class of error turns out to be frequent enough to warrant it.
- **Root-caused and fixed the real multi-minute/indefinite tool-call hangs (2026-07-04)**, live-reproduced via direct SQL against the observability DuckDB against an in-progress run: Mastra's built-in `grep` workspace tool is a pure-JS recursive filesystem walk (one E2B `readdir`/`stat`/`readFile` network round-trip per file and directory, fully sequential, no concurrency, no timeout, no file-count cap on the walk itself — only the 1000-match *output* cap). On a large tree (e.g. a freshly `git clone`d Rails app), three parallel `grep` calls in one step measured live at 4m20s, 6+ minutes, and 6+ minutes (still open when checked). This is almost certainly also what explains the earlier "gorkie forgets a tool call for 30 minutes then resumes" reports. Fixed by disabling the built-in tool (`enabled: false` on `WORKSPACE_TOOLS.FILESYSTEM.GREP` in `workspace/index.ts`) and replacing it with `tools/grep.ts`, a custom tool with the same id/name and near-identical input schema that shells out to real `rg` inside the sandbox via `execute_command`'s underlying `sandbox.e2b.commands.run`, with a 30s internal timeout and an output-size cap. `ripgrep` was already installed in the sandbox template. Updated the tools prompt's `grep` note accordingly (no longer says "avoid it, use execute_command instead" — it now *is* execute_command-backed).
- **Found and fixed a second, compounding observability bug**: `src/mastra/index.ts`'s DuckDB path was a bare relative string (`'./observability.duckdb'`). Confirmed directly in `node_modules/mastra/dist/index.js` that `mastra dev`'s actual server process runs with `cwd` set to its own dev public dir, and `mastra start` spawns `node index.mjs` with `cwd: .mastra/output` — neither is the repo root, so the same relative path silently resolved to different files per mode, and it happened to land inside `src/mastra/public/` (a directory `mastra build` treats as static assets to copy, which is also the root cause of the previously-tracked 9.1 GB build disk-space blocker, now also fixed as a side effect). Fixed by anchoring to `process.env.MASTRA_PROJECT_ROOT`, which both CLI paths already inject as the true repo root for exactly this class of problem. Moved the existing 12 GB file (same filesystem, instant `mv`) to the new stable path so history isn't orphaned. Confirmed via direct SQL that a specific historical "frozen" thread has zero rows in this DB — its session was writing to a different, since-regenerated `.mastra/`-internal file that no longer exists on disk; that specific historical trace is unrecoverable, but the underlying bug (and the reason it was unrecoverable) is now fixed going forward.
- Added an `AGENTS.md` rule: never start, restart, or kill `mastra dev`/`mastra start` on your own initiative — this is a live Slack bot the user runs themselves, and two instances racing for the same Slack Socket Mode connection causes real, confusing failures (confirmed one such live overlap this session, unrelated to the above).
- Batch of fixes (2026-07-04, later): restored `scripts/verify-mastra-patch.ts` and the `postinstall` hook in `package.json` (both had gone missing — a real regression, since this is the safety net that catches `@mastra/core` silently installing unpatched; confirmed working again via a clean `bun install`). Fixed the completion-footer bug where `skip` suppressed the footer even if the model had already written a real reply earlier in the same turn (`turns.ts` now gates on `result.text.trim().length > 0`, the actual accumulated reply text, instead of "was `skip` called anywhere this turn"). Elevated repository transfer, collaborator changes, secret changes, and user-data deletion from "confirm first" to an absolute refusal tier in `guardrailsPrompt` — no phrasing or claimed authority unlocks them, including claiming to be gorkie's own owners; genuine owners are told to do these directly rather than through gorkie. Added a repo-ownership/no-LICENSE-file rule to the `gh-cli` skill for repos gorkie creates (owned by gorkie, @twa, @Devarsh). Added emoji download to `get_file` (shortcode like `:partyparrot:` → resolved via `emoji.list`, following alias chains; needs the `emoji:read` scope added to the app manifest and a reinstall to actually work — not currently granted). Reverted `create_scheduled_task`'s min-interval check from `croner` back to Mastra's own `validateCron`/`computeNextFireAt` (the dependency had crept back in from an interrupted parallel session) and removed `croner` from `package.json` again. Restarted the live server on `mastra dev` after finding it had drifted onto a stale prebuilt `.mastra/output` bundle from before all of today's changes.
- Added a dedicated `guardrailsPrompt` system section (2026-07-04) covering destructive repository and production actions, harmful outbound messages, risky installs and tunnels like sshx or Termux-style remote shell workflows, and mandatory visible progress updates for sandbox, GitHub, browser, and deployment work. The `agent-browser` skill now also requires screenshots, narration, and confirmation before risky browser actions.
- Reversed the same-day subagent removal (2026-07-04): restored `agents/research.ts`, `agents/explore.ts`, `processors/delegates.ts`, the `agents: { research, explore }` block on `gorkieAgent`, `delegates` in `outputProcessors`, the `agent-*` display-name regex in `chat/tool-display.ts`, and the `agent-research`/`agent-explore` tools-prompt section, all restored verbatim from the last commit. Both helper agents now also get `fetch_url` in their `activeTools` (added alongside `search_web`), so research doesn't need the full workspace just to read a link, and explore doesn't need a redundant fetch path duplicated outside its read-only scope. The `Delegate long-run failure state` and `Subagent stability` P0s are back too, since the underlying bugs were never fixed, only sidestepped by removing the feature.
- Deduplicated the Exa client into `lib/exa.ts` (was instantiated separately in `search-web.ts` and `fetch-url.ts`); both tools now import the shared `exa` singleton.
- Reverted the sandbox-proxy experiment entirely (2026-07-04, same day it was verified working): removed `SANDBOX_PROXY_URL` from `env.ts` and `.env`, reverted `config.sandbox.template` to `gorkie-workspace:1.2`, and restored `build-template.ts` to a single plain build (no `proxiedTemplate`/`waitForPort` code, no SOCKS relay binary). The already-built `gorkie-workspace-proxied:1.2` E2B template alias is now unreferenced by any code; delete it from the E2B dashboard if desired. Existing paused sandboxes created while the proxied template was default are untouched (real per-thread state) and will keep working on that image until naturally replaced.

- Fixed the completion footer posting to fake Studio thread ids and throwing `ValidationError`: `processOutputResult` (`processors/turns.ts`) now checks `channelContext().platform === 'slack'` (populated for real Slack turns via Mastra's own `buildEventContext`, matches our Studio preset's `platform: 'studio'`) before attempting to post, so non-Slack contexts skip it entirely instead of erroring.
- Confirmed live in Slack: the `read_file` tool-result image relocation fix works end to end — the model correctly describes actual image content (not a filename-based guess) via `read_file` on both the `opencode-go` and `openrouter` routes. The corresponding E2E testing item is closed.
- Batch of UX/safety fixes (2026-07-04): `post_message` catches Slack `channel_not_found`/`not_in_channel` and returns actionable "Gorkie must be invited (`/invite @gorkie`)" errors (private channels surface as `channel_not_found` to non-members; un-joined public channels give `not_in_channel` unless the bot has `chat:write.public`); the tools prompt got a strong "consider delegating BEFORE multi-step research/exploration" section (gorkie kept forgetting helper agents exist) plus channel-membership and min-interval notes; the agent-browser skill now tells the agent to treat the user as a collaborator (short step narration, screenshots of key steps via `upload_file`, view its own website screenshots with `read_file` before declaring success, prefer session recordings for multi-step flows); `generate_image` filenames now include a per-call batch suffix so repeat calls stop overwriting earlier images; AGENTS.md documents the DuckDB observability database as the debugging ground truth.
- Closed the `post_message`/`upload_file` write-side access gap (flagged live by a beta tester: "can a bad actor do this?"): both tools previously only required *Gorkie* to be a member of the target channel, not the requesting user, so anyone who could DM/mention Gorkie could have it relay a message into any channel Gorkie belongs to. Fixed via `assertCanPostTo` (`tools/slack/utils.ts`): for any target other than the current thread, checks `conversations.members` for the target channel and refuses if the requesting user isn't in it. Deliberately does not cover `target.type === 'user'` (DMing a third party on the requester's behalf) since "channel membership" doesn't apply to a 1:1 DM — that's a related but distinct risk still needing its own design, see the private-read-approvals item above.
- Enforced the scheduled-task minimum interval in code, not just the prompt: `create_scheduled_task` now uses `croner` to sample the next 5 fire times and rejects if any gap is under 30 minutes (`scheduledTasks.minInterval` in `config.ts`). Sampling multiple fires (not just the first gap) matters for irregular crons like `0,5 * * * *`, where checking only the next occurrence could see a long gap while a short one lurks later in the cycle, making the same cron string pass or fail depending on the clock.
- Simplified `scripts/postbuild.ts` to run `bun install` itself (via `Bun.spawnSync`) instead of shelling out with `cd .mastra/output && bun install`, so the `postbuild` package.json script is just `bun run scripts/postbuild.ts`.
- Confirmed `scripts/verify-mastra-patch.ts` (the `postinstall` safety check) actually catches a real failure mode: `bun install` with a `patchedDependencies` key that doesn't match the installed `@mastra/core` version succeeds with zero warning and silently installs the *unpatched* package. Verified experimentally that the check fails loudly in that scenario and passes cleanly otherwise.
- Verified via the running dev server's observability API (DuckDB is writer-locked while `mastra dev` runs, so queried `http://localhost:4111/api/observability/traces` instead): across the 9 most recent agent runs, max `inputTokens` was ~48k of the 200k budget with zero error spans — no token overload after re-enabling `read_file` media.
- Implemented the `read_file` image fix: `relocate-tool-result-images` `CompatRule` in `processors/tool-media.ts` (relocates `image/*` `media` parts from tool results into a synthetic user message with a `file` part, opencode-inspired), registered via `ProviderHistoryCompat` on `gorkie` and `explore`, re-enabled `mediaTypes` on `read_file`, and updated the `read_file` prompt note (images visible, PDFs not). Statically verified for both the bundled v2 OpenRouter and vendored v2 OpenAI-compatible (opencode-go) converters; awaiting live Slack E2E.
- Bumped `@mastra/core` to `1.50.0-alpha.1` (and `@mastra/observability` to `1.16.0-alpha.0` for its new `flush` interface requirement) and shrunk the patch to `patches/@mastra+core@1.50.0-alpha.1.patch` containing only the token-limiter fix. Upstream [PR#18396](https://github.com/mastra-ai/mastra/pull/18396) (merged 2026-06-25, in alpha.1) made the local `applyStoredModelOutputsPatch`/`normalizeModelOutputPatch` half redundant. Also corrected the loop-path finding: the outgoing prompt is built by `llmPrompt()` (which had the substitution since alpha.0), not `model()`; the remaining `read_file`-image blocker is the bundled spec-v2 OpenRouter model's `getToolResultContent` stringifying all non-text tool output, so the planned `ProviderHistoryCompat` relocation fix is still required. Updated `scripts/verify-mastra-patch.ts` to the new chunk names (`chunk-B3SHPOO6.js`/`chunk-P6SA5MPH.cjs`); `bun install` + postinstall verify + `bun run typecheck` all pass.

- Fixed `bun run build` silently shipping an unpatched `@mastra/core`: `mastra build` generates its own `.mastra/output/package.json` and runs a fresh `bun install` inside `.mastra/output`, which ignored the root `patchedDependencies`/`patches/` entirely, so the deployed bundle had neither the token-limiter fix nor the `toModelOutput` multimodal fix even though local dev (using the root `node_modules`) had both. Added `scripts/postbuild.ts` plus a `postbuild` script (bun auto-runs it after `build`, same as npm) that copies `patches/` into `.mastra/output` and merges `patchedDependencies` into its generated `package.json`, then re-runs `bun install` there so the patch actually applies. Verified both fixes are present in `.mastra/output/node_modules/@mastra/core` after a full `bun run build`.
- Added `generate_image`: generates AI images (`google/gemini-3.1-flash-image` via the Hack Club OpenRouter-compatible proxy) and always writes them into the sandbox. Delivery is left to `upload_file` (already has full thread/channel/user target support) rather than duplicating destination logic in `generate_image` itself. Model gateway config lives in `providers.ts` as `imageGenerator`, a single config (no cross-gateway fallback — deliberately kept simple; add fallback later if the single gateway proves unreliable).
- Confirmed the "sent on behalf" attribution finding (above) applies equally to `upload_file`: it uses the identical `withAttribution(..., userId, isCurrentThread)` call with the same `channelContext().userId`.
- Confirmed "sent on behalf" attribution (`withAttribution` in `chat/attribution.ts`) already credits whoever sent the *current* triggering message, not the thread starter: Mastra's own `channelContext().userId` is rebuilt per inbound message from `buildEventContext({ actor: message.author })` (see `chunk-6NSMFZFB.js`'s `processChatMessage`/`buildEventContext`), so it's already "who asked the agent to send this," not the resourceId/thread creator. No code change needed; flag a concrete repro if this is ever observed wrong in Slack.
- Standardized Slack ids: removed `normalizeTarget()` (the flexible raw-or-prefixed input shim in `chat/target.ts`) since the `chat` package's own canonical id scheme is already `slack:C...`/`slack:C...:ts` everywhere (confirmed in `chat`'s source docs). `post_message`/`upload_file`'s `targetSchema`, and the `channelId` params on `get_channel_info`/`list_threads`/`read_conversation_history`, now explicitly require the standardized form in their descriptions, plus a shared note in the tools prompt telling the model to reuse ids exactly as seen in other tool outputs rather than reformatting them.
- Fixed sandbox git identity: `build-template.ts`'s image-build `git config --global user.email` and `network.ts`'s `GIT_AUTHOR_EMAIL`/`GIT_COMMITTER_EMAIL` now use `gorkie@agentmail.to` (Gorkie's actual GitHub/AgentMail address) instead of the placeholder `gorkie-agent@users.noreply.github.com`.
- Updated the `read_file` tool note (now that media works) and the `agent-browser` skill stub to say screenshots/images can be viewed with `read_file`, not just downloaded.
- Re-enabled `read_file` sandbox media (removed `mediaTypes: false` in `workspace/index.ts`) after patching the root cause: Mastra's `createTool`/`toModelOutput` multimodal bug ([mastra-ai/mastra#18400](https://github.com/mastra-ai/mastra/issues/18400), fix in open PR [#18418](https://github.com/mastra-ai/mastra/pull/18418), not yet merged/released). Root cause: `MessageList.all.aiV5.model()` — the path the non-durable agent loop uses — never substituted a tool's stored `toModelOutput` (`providerMetadata.mastra.modelOutput`) into the tool-result part sent to the model, so `read_file`'s image/media output was replaced by the model with the raw tool return value (which just describes the file, not the actual image bytes). Ported the PR's `applyStoredModelOutputs` fix by hand into the installed dist chunks (`chunk-TQ2PVBZA.js` / `chunk-VUSAEB2W.cjs`) as `applyStoredModelOutputsPatch`, wired into both `model()` and `prompt()`, added to the same `patches/@mastra+core@1.50.0-alpha.0.patch`. Verified the transform logic in isolation with a simulated `read_file` image result. Should be dropped once the upstream PR ships in a release.
- Bumped `@mastra/core` to `1.50.0-alpha.0` and patched (via `bun patch`, see `patches/@mastra+core@1.50.0-alpha.0.patch`) a real upstream bug in `TokenLimiterProcessor.countInputMessageTokens`: it counted image/file message parts by `JSON.stringify`-ing the entire part, including the base64 payload, as literal text tokens. A single image attachment could look like 100k+ tokens, so `trimMode: 'contiguous'` evicted the whole conversation (including the message with the image) to fit the budget, producing "No user or assistant messages in the request". The patch counts a small fixed token cost per file/image part instead of the base64 data. Image attachments are intentionally left in `message.attachments` (not stripped) so they still reach the model. Still open: models/routes with no vision endpoint (see P0 above).
- Updated the file/image token-count patch (2026-07-05) from a flat guess (765 tokens for any image, 258 for any other file, regardless of actual size) to a byte-size-scaled estimate: reads `part.url`'s base64 payload length (real field per the AI SDK's `FileUIPart` type — confirmed via `node_modules/ai/dist/index.d.ts`, not guessed), converts to decoded byte size, divides by 750, clamps to `[200, 4000]`, and only falls back to the old flat constants when no `url` data is present at all. Not billing-accurate (every real provider tokenizes images differently — OpenAI tile-based, Anthropic `⌈w/28⌉×⌈h/28⌉`, both verified live against current docs — and this has no access to real pixel dimensions without decoding), but proportional rather than flat, so a tiny icon no longer costs the same budget as a 20MB photo. Verified both patched dist chunks still parse (`esbuild.transformSync`) and the estimate scales/clamps sensibly against synthetic base64 payloads of varying size. Candidate for the upstream Mastra PR discussed below (byte-size default, with an escape hatch for callers who want to supply their own per-part estimator).
- Normalized raw Slack upload/post targets so `C...` channel ids become `slack:C...`, raw thread ids become `slack:C...:ts`, and user ids stay suitable for `openDM`.
- Kept Slack read tools conservative after prompt-injection concerns: current conversation and public workspace channels only until private-read approvals are designed.
- Added Slack timezone fields to `get_user` and updated scheduled-task prompting to infer the requester's IANA timezone from their profile for time-of-day schedules.
- Confirmed the AgentMail skill already treats `gorkie@agentmail.to` as Gorkie's official/default inbox.
- Configured sandbox Git commit identity: new E2B templates write `gorkie-agent` name/email to global Git config, sandbox env includes `GIT_AUTHOR_*` and `GIT_COMMITTER_*`, and the GitHub contribution skill repairs identity in existing sandboxes before committing.
- Fixed sandbox Git HTTPS auth by brokering `github.com` with Basic auth while keeping GitHub API hosts on Bearer auth, and updated the `gh-cli` skill so external contributions fork first and push PR branches to the fork.
- Confirmed the execute helper agent is gone from the registered helper-agent set and prompts; only Research and Explore remain.
- Confirmed `search_slack` catches thrown Slack `invalid_action_token` / `token_expired` errors, clears the thread search token, and returns a model-readable prompt to ask for a fresh mention.
- Styled the completion footer as italic Slack mrkdwn and confirmed the current footer path has no steering-detection state. It posts from Mastra's final `processOutputResult` hook only after a run resolves.
- Killed all active `mastra dev` / `.mastra/output` runtime processes for this checkout, then verified port `4111` and `observability.duckdb` have no open listeners or locks.
- Refactored `delegate_task` cancellation/display: abort now stops the heartbeat loop and marks the parent task as cancelled, child stream abort chunks stop the delegate loop, and the parent Slack card title stays `Starting Task: ...` through completion.
- Confirmed scheduled tasks are created with the current channel `threadId` plus agent `resourceId`, so runs wake or persist back into the same Slack context where the task was scheduled. Access currently allows the same resource or same thread.
- Audited the Mastra dev/HMR path. The running dev server hot-reloaded successfully, but a second dev/start process fails because the existing dev child owns port `4111` and the DuckDB write lock.
- Removed the temporary steering tracker from Slack handlers. The completion footer now posts from the Mastra result hook without steering detection.
- Restored the last committed delegate-task display behavior for the Slack "Starting Agent" card.
- Restored the older ranged `.part`/`.next`/`.merge` `get_file` downloader because Slack files are often huge and interrupted retries should resume.
- Added the completion footer through Mastra's `processOutputResult` hook: non-steered Slack turns post `Gorkie may make mistakes. Double-check important information.`, and `skip` gets the footer for now.
- Scrapped the completion-divider experiment completely after Slack rejected divider-only messages with `no_text`.
- Removed the experimental turn-status reaction code and keep `skip` visually silent by hiding its tool row.
- Explained why steering can show a tick: Mastra's channel handler can return after delivering a new message into the active run, while `respond()` unconditionally marks that message `done` after `defaultHandler` returns.
- Updated `@mastra/e2b` to `0.5.0-alpha.0`, switched sandbox creation to direct `new E2BSandbox({ network, ... })`, renamed the sandbox helper functions to `createNetwork` and `createEnv`, and removed the `GorkieSandbox` post-start `updateNetwork` wrapper path.
- Wrote `CODING_STANDARDS.md` and did a standards-driven cleanup pass: replaced the `Sandbox.create` monkeypatch with `e2b.updateNetwork()` applied after start in `GorkieSandbox` (works until the `@mastra/e2b` network option ships), centralized the `as E2BSandbox` casts into `resolveE2BSandbox()`, removed the redundant toolCallId/toolName Maps and duplicated stream branches in `delegate_task`, deduped `label()` into `lib/label.ts`, deleted dead `successToolCall` and the unused `config` wrapper, folded the repeated heartbeats cast and task scoping into `scheduled-tasks/queries.ts`, and split `get_file`'s 170-line closure into named download functions.
- Checked the old reference scheduled-task implementation. It is a recurring automation system, not just one-shot reminders: `scheduleTask` creates cron jobs, `listScheduledTasks` and `cancelScheduledTask` manage them, App Home renders/cancels them, and a background `scheduledTaskAgent` runs each task and calls `sendScheduledMessage` exactly once.
- Refined the scheduled-task plan to be AI-facing only: create/list/pause/resume/delete, no App Home task UI, and default delivery to the Slack place where the task was scheduled.
- Confirmed Mastra PR [#18785](https://github.com/mastra-ai/mastra/pull/18785) was merged at `2026-07-02T10:06:03Z` as commit `cb7a7218`. NPM is still at `@mastra/e2b@0.4.1`, so local cleanup waits for release or explicit vendoring.
- Vendored the Mastra PR version of `@mastra/e2b` into Gorkie, temporarily simplified sandbox creation to direct `new E2BSandbox({ network, ... })`, and verified `bun run typecheck` passes. Updated the Mastra changeset and PR body with the E2B per-host request transform docs link.
- Opened Mastra PR [#18785](https://github.com/mastra-ai/mastra/pull/18785) adding a clean `network` option to `@mastra/e2b`, with an E2B unit test and focused package verification.
- Explained why sandbox network options are registered in a map before patching `Sandbox.create`.
- Fixed E2B brokered credential egress by applying AgentMail/GitHub `network.rules` at sandbox creation time through a small `NetworkedSandbox` wrapper.
- Fixed Python AgentMail certificate failures by passing `SSL_CERT_FILE=/usr/lib/ssl/cert.pem` through the command env. Smoke check returned `agentmail:ok`.
- Removed brokered bearer injection from `github.com` root. Smoke check confirmed public git over `github.com` works and `gh api` still authenticates as `gorkie-agent` through `api.github.com`.
- Updated `delegate_task` progress so the parent task card reads `Starting Research Agent: Calm Green`, heartbeats update the same card, and child tool calls update details such as `Running Search Slack`.
- Verified no Mastra or `.mastra` runtime processes are still running.
- Stopped the local `mastra dev` Gorkie runtime process group.
- Added host-side E2B network-rule credential brokering for AgentMail and GitHub without putting real tokens into sandbox environment variables.
- Added input token limiters to Research, Explore, and Execute so delegated loops get the same prompt-size guardrail as the main agent.
- Changed delegated child tool rows to render under agent prefixes such as `[Research] Search Slack`, and changed logs to `[agent] [research] ...`.
- Added unique delegated run names via `unique-names-generator` for concurrent Research, Explore, and Execute runs while keeping tool titles grouped by agent type.
- Researched cleaner E2B credential-injection paths in the installed Mastra/E2B APIs. `@mastra/e2b` does not expose a direct `network` option, so create-time `Sandbox.create` wrapping is needed for E2B transform rules.
- Removed Node AgentMail install/reference paths. AgentMail skill docs are now Python-only, and the template still keeps Node plus `gh`.

## Testing (E2E, needs you in Slack)

- [ ] **Slack target/read scope E2E**: verify `upload_file`/`post_message` work with standardized `slack:C...`/`slack:C...:ts` ids (raw `C...`/`U...` ids are no longer auto-normalized after removing `normalizeTarget`), current-conversation reads still work, and private reads outside the current conversation are denied.
- [ ] **`generate_image` E2E**: verify it writes images into the sandbox and that a follow-up `upload_file` call sends them to the current thread (default) and to an explicit target, and confirm the Hack Club `google/gemini-3.1-flash-image` route actually returns images (not just text).
- [ ] **`generate_image` image-editing — root cause fully diagnosed and confirmed (2026-07-05), not fixable client-side**: added an `imagePath` param (AI SDK's real `generateImage({ prompt: { images: [...], text } })` edit-mode shape, verified against `ai@6.0.219`'s types), then removed it after live-testing found it can't work through hackclub/ai's proxy. Two independent, now fully-confirmed blockers, found by building a real reproduction loop (per `/diagnosing-bugs`) rather than guessing:
  1. **No `/images/edits` route exists.** Confirmed via full commit-history and code search across `hackclub/ai` — only `/images/generations` has ever existed. The AI SDK's edit-mode prompt targets `/images/edits` under the hood, so it 404s.
  2. **A raw `/chat/completions` + `modalities: ['image','text']` workaround (mirroring hackclub's own internal implementation, and confirmed to be exactly how an older working version of this same bot — `imdevarsh/gorkie`'s `v1` branch, using `@openrouter/ai-sdk-provider@2.9.0` — used to do image editing before it stopped working around 2026-05-31) is now also structurally blocked.** Root cause, confirmed with a 6-attempt reproduction loop (100% failure, ~5.9-6.9s each) plus a minimizing follow-up: `hackclub/ai`'s `handleProxy` (backing `/chat/completions`, `/responses`, `/embeddings`) wraps its upstream call in `fetchWithHeaderTimeout`, a hardcoded 5-second header timeout added in `4c8f933b` ("Add upstream header timeout (#110)", merged 2026-05-24) — timing lines up with the reported "last worked May 31" almost exactly (a few days for the merge to reach their production deploy). `/images/generations` has no such wrapper (bare `fetch()`), which is why generation still works. Confirmed via a follow-up test that this fails identically even for **pure text-only generation** (no image input at all) through `/chat/completions` with `modalities` set — so it was never specifically about image editing, just about that route's timeout being too short for any multimodal-image response from this model. This is a hardcoded server-side limitation on hackclub's own infrastructure; nothing on our end can work around it. Revisit only if hackclub/ai raises or removes that timeout for image-capable models, adds a real `/images/edits` route, or if we wire in a direct Gemini API credential (confirmed Gemini 2.5/3.x Flash Image natively supports conversational image editing, and has a free tier) as a separate provider instead of routing through hackclub/ai's proxy. Worth reporting upstream to hackclub/ai as a real bug, given we have a precise, reproducible root cause and have contributed merged PRs there before.
- [ ] **`OPENROUTER_API_KEY` is mislabeled — it's a second Hack Club key, not a real OpenRouter key**: found while debugging the above. Both `HACKCLUB_API_KEY` and `OPENROUTER_API_KEY` in `.env` start with `sk-hc-` (Hack Club's own prefix) and are the same length; a real OpenRouter key is prefixed `sk-or-v1-...`. `providers.ts`'s `gateways()` uses `OPENROUTER_API_KEY` against genuine `https://openrouter.ai/api/v1` as a fallback rung for the orchestrator/summarizer models — with a Hack Club key in that slot, that fallback would almost certainly fail with an auth error if the primary `opencode-go` route and Hack Club's own proxy route both fail simultaneously, silently defeating the 3-tier fallback chain's whole purpose. Needs a real OpenRouter-issued key put in that slot (or the fallback rung removed if not wanted).
- [ ] **Scheduled task scope/access E2E**: in Slack, create/list/pause/delete from a channel thread and a DM, confirm tasks wake in the creation conversation, same-thread users can manage thread tasks, and unrelated users/DMs cannot.
- [ ] **Scheduled task delivery E2E**: once fixed, verify a scheduled task fires and delivers inline in a channel thread, in a DM, and that a task whose instructions say to post elsewhere uses `post_message` to the right target. The 30-minute minimum is now enforced in code (`create_scheduled_task`, see Recently completed), so this no longer needs a model-compliance check — just confirm an under-30-minute request is rejected with the code-level error, not a model refusal.
- [ ] **Images without extensions**: fix `read_file` on downloaded images/binaries without file extensions so it detects MIME from bytes instead of dumping raw binary text.
- [ ] **DM** conversation (gorkie replies to every message).
- [ ] **Observational Memory + compaction**: long thread, confirm older history compresses and recall still works.
- [ ] **`get_file` timeout retest**: after the cleanup refactor, repeat the huge Slack zip forced-abort test and confirm `.part`/`.next` resume still completes with `unzip -t`.
- [ ] **Real Slack `get_file` retest**: test current `get_file` against an actual Slack file in E2B, including a previously paused/dead sandbox, and confirm completion is indicated clearly.
- [ ] **E2B start regression check**: verify whether `ensureRunning()` fails on paused/dead sandboxes where `start()` succeeds, especially for `get_file`, `upload_file`, and filesystem init.
- [ ] **Foreground `execute_command` stale sandbox recovery**: reproduce the post-turn paused-sandbox case where the first foreground command returns `Sandbox is probably not running anymore`, then confirm whether wrapping `executeCommand` itself in `retryOnDead` fixes the first command after a pause.
- [ ] **Slack search compact-output test**: after researching a compact `search_slack` model output shape, test repeated broad Slack searches in one turn and confirm the model sees concise hits plus cursors instead of giant context JSON.
- [ ] **Token limiter loop test**: after researching the Mastra `TokenLimiterProcessor` settings, run a tool-heavy multi-step turn and confirm per-step pruning prevents context overflow without deleting the active user request or latest tool result.
- [ ] **Observational memory token-limit test**: test whether an input token limiter interferes with observational memory, long-thread recall, or observation/reflection writes, especially when older messages are pruned from the live model prompt.
- [ ] **Mention input/output rendering**: confirm Slack mentions resolve properly in model-visible input, thread context, history/tool output, and final Slack replies.
- [ ] **Filesystem tools in Slack**: confirm `read_file`, `write_file`, `edit_file`, `list_files`, `grep`, `delete_file`, `file_stat`, and `mkdir` work through Slack and render cleanly.
- [ ] **Full tool robustness audit**: review all repo tools plus installed Mastra workspace tools for direct E2B access, stale sandbox recovery, path handling, and output/media behavior.
- [ ] **All tool smoke tests**: test every registered tool in Slack, including `getUser`/user lookup behavior.
- [ ] **Tool caching parity**: validate caching behavior for all tools against the reference code.

## Tools

- [ ] **Custom voice support**: support user-selected/custom voices instead of only a default Google Assistant-style voice.
- [ ] **Slack voice message sending**: send generated voice messages directly back to Slack when approved.
- [ ] **Reusable sandbox skill system**: explore `skills.sh` or similar systems, and prefer reusable preinstalled sandbox skills over ad hoc installs.
- [ ] **User MCPs**: allow users to connect their own MCP servers safely.
- [ ] **User-defined skills**: allow users to define skills with review, scope, and safety limits.
- [ ] **`search_slack` action-token lifetime investigation**: measure how long Slack `action_token`s remain valid, why they die between turns/tool calls, and whether token storage should be per-message, per-thread, refreshed from new mentions, or replaced by a user-token search path.
- [ ] **`search_slack` token plumbing cleanup**: the `captureSearchToken`/`getSearchToken` flow (grab the per-message Slack `action_token` from `message.raw` in the handlers, stash per-thread, read it back in the tool) is hacky. Find a cleaner way to plumb the action token to the tool.

## Prompts

- [ ] **App Home / Assistant suggested prompts**: `channels.sdk.onAssistantThreadStarted` → `slack.setSuggestedPrompts(...)`. Part of the App Home phase.
- [ ] **User custom instructions**: inject the speaker's saved preset as `<user_instructions>` in the user message (not the system prompt, to keep caching). Core prompt already references it.

## Data / persistence

- [ ] **Port the reference's drizzle setup** (`gorkie-slack/apps/*`): schema + migrations + client for **custom instructions / presets**, allowlist/onboarding, App Home state. Mastra's `PostgresStore` owns memory/observability tables; this is *our* app data on the same shared Supabase Postgres. Decide: reuse the reference's tables or namespace gorkie's. Blocks: custom instructions, App Home presets, allowlist. Note `:6543` is the pooler, migrations may need the `:5432` direct URL.

## Features

- [ ] **Add more skills**: expand available skills after current skill loading is fixed.
- [ ] **Self-learning and auto-skill creation**: explore whether Gorkie can create reusable skills from repeated work without becoming too specific, surprising, or invasive.
- [ ] **Multi-model support**: consider Gemini/GPT/Claude routing, gated by cost controls and explicit user choice.
- [ ] **Website creation and deployment workflows**: support creating, previewing, and deploying websites for users.
- [ ] **Agent browser**: consider adding a proper agent browser integration instead of relying on ad hoc browser tooling.
- [ ] **`[Thread context]` mention encoding**: Mastra's auto-injected thread-context block HTML-escapes mentions (`&lt;@U…&gt;`) so the model reads escaped junk (our `read_conversation_history` output is clean). Decide: disable `threadContext` (`maxMessages: 0`) + rely on the tool, override the block, or report upstream.
- [ ] **App Home**: `sdk.onAppHomeOpened()`; persona presets + per-user custom instructions store.
- [x] **Allowlist / onboarding — ported (2026-07-07)**: opt-in gating ported from the reference bot (`/workspaces/gorkie-slack/apps/bot/src/lib/{allowed-users,onboarding}.ts`) to Mastra channels. New `OPT_IN_CHANNEL` env var (optional, feature is a no-op when unset — everyone allowed, matching prior behavior). `lib/allowed-users.ts`: `isUserAllowed`/`addAllowedUser`/`buildAllowlist`, cached via `chat().getState()`, kept live via a second `onMemberJoinedChannel` listener (confirmed `chat`'s handler list is an array, so this coexists with the existing "hey I'm hanging out here" greeting handler without conflict). `chat/onboarding.ts`: `offerOptIn` (ephemeral card with an "I accept" button) / `acceptOptIn` (grants access, invites to the opt-in channel, confirms). Wired into `chat/handlers.ts`'s `onMention`/`onDirectMessage` (offers opt-in on first unauthorized contact) and `onSubscribedMessage` (silently blocks further messages without re-offering, since the card was already shown once). `buildAllowlist()` called at startup in `index.ts` after `setChat`/`registerEvents`. `bot.onAction('opt_in_accept', acceptOptIn)` registered in `chat/events.ts`. Typechecked and linted clean. **Not yet live-tested** — needs a real `OPT_IN_CHANNEL` set and a non-member account to confirm the full flow (blocked → offered → accepts → invited → unblocked).

## Research

- [ ] **`gorkie (dev)` credit exhaustion — recurred (2026-07-07), self-healed via fallback**: live-observed again — the Hack Club proxy rung (`https://ai.hackclub.com/proxy/v1`) rejected a `minimax/minimax-m3` call with a 402 ("requested up to 32768 tokens, but can only afford 10594... adjust the key's daily limit"), i.e. the shared Hack Club key hit its daily budget for that request size. Not fatal this time: `providers.ts`'s gateway fallback chain (hackclub → openrouter → opencode) retried and the turn completed successfully a few seconds later ("[turn] final finished" right after). Still worth deciding: whether to request a higher daily limit on the shared key, or lower `maxOutputTokens` for cheaper/high-volume models like minimax-m3 so they need less headroom per call and hit this less often.
- [ ] **`mastra dev` shutdown races its own background workflow storage against pool closure — upstream bug, not ours**: on `SIGINT` (Ctrl+C), live-observed `Output processor workflow failed ... Cannot use a pool after calling end on the pool` repeated several times. Traced: gorkie's own code has zero signal handlers (confirmed — no `SIGINT`/`SIGTERM`/`.close()` anywhere in `src/mastra/index.ts`), so this is entirely internal to the `mastra` CLI's own graceful-shutdown sequence. Every agent output-processor run is tracked internally as a durable workflow (named `<agentId>-output-processor`, confirmed in `@mastra/core`'s `chunk-XJNAKXUS.js`'s `executeWorkflowAsProcessor`), and that workflow's `getWorkflowRunById` call hits the Postgres pool *after* the CLI has already called `.end()` on it during shutdown — an ordering bug in Mastra's own shutdown sequence, not something gorkie causes or can fix locally. Low severity: only fires on dev-server stop, doesn't affect live serving. Worth reporting upstream if it gets noisy enough to matter; not blocking anything today.
- [ ] **Product ideas backlog**: keep online form filling, website builder/deployer, GitHub repo debugging assistant, Minecraft bot/server agent, guru/life-advice mode, and investing/cooking/flying-plane jokes as later ideas until explicitly prioritized.
- [ ] **Model output-token budgeting**: check whether high `maxOutputTokens` changes Mastra observability, memory persistence, provider routing, or agent behavior beyond provider preflight/billing checks; also research whether OpenRouter can avoid rejecting requests based on reserved output-token affordability.
- [ ] **Built output env loading**: confirm whether Mastra build intentionally drops `dotenv/config` from the standalone output, and decide whether local smoke should use `node --env-file=.env .mastra/output/index.mjs` or an explicit runtime env loader.
- [x] **Mastra build disk-space blocker — fixed as a side effect (2026-07-04)**: root cause was `src/mastra/index.ts`'s DuckDB `path: './observability.duckdb'` being a bare relative path. `mastra dev`'s actual server process runs with `cwd` set to its own dev public dir, and `mastra start` spawns `node index.mjs` with `cwd: .mastra/output` (confirmed directly in `node_modules/mastra/dist/index.js`'s `start()`) — neither is the repo root, so the same relative path silently resolved to two different files depending on which mode ran, and it happened to land inside `src/mastra/public/`, a directory `mastra build` treats as static assets to copy. Fixed by anchoring to `process.env.MASTRA_PROJECT_ROOT` (which both CLI paths already inject as the true repo root, specifically for this class of problem) via `join(projectRoot, 'observability.duckdb')`, and moved the DB out of `public/` to the repo root. The existing 12 GB file was `mv`'d (same filesystem, instant) to the new path so trace history isn't orphaned. `mastra build` no longer sees a multi-GB file under `public/` to copy at all.
- [ ] **Re-test screenshots/Playwright** now that the sandbox persists and the template pre-installs agent-browser.
- [ ] **Sandbox-lifecycle bump, raise the duration?** `processOutputStep` runs *before tool execution* (per Mastra docs), so our `e2b.setTimeout(SANDBOX_MS)` is already proactive, but `SANDBOX_MS` is only **5 min**, so a single `execute_command` running >5 min could pause mid-run. Reference uses ~21 min (`executionTimeoutMs + 60s`). Consider raising `SANDBOX_MS`; we `pause()` at turn end anyway, so the timeout is just an abandon-safety net.
- [ ] **Channels escape hatch**: if channels' all-in-one handling ever limits us, evaluate owning those pieces.
- [ ] **Dev vs. prod separation — researched, partially decided (2026-07-07)**: checked Mastra's own docs/CLI for a built-in "environment" concept — there isn't one; `mastra dev` (Studio) vs `mastra build`+`mastra start` (production) is purely a *run mode* distinction, not an environment/config split. Confirmed there's also no Mastra-hosted general-purpose database offering (checked `@mastra/core`'s bundled storage reference docs — every listed adapter, Postgres/libSQL/MongoDB/DuckDB/ClickHouse/etc., is self-hosted or your own cloud account; Mastra Platform is observability/tracing-only, not a data store). So "two Mastra projects" isn't really the right framing for storage — you don't need two codebases, you need two **configs** pointing at different backing services. **Decided**: at minimum, two separate Mastra Platform projects (dev + prod `MASTRA_PROJECT_ID`/access token) so dev traces don't pollute the prod observability dashboard. **Still open**: whether to also split the Postgres database (or just a schema) so dev testing can't corrupt prod memory/scheduled-tasks/allowlist state — a separate Slack app (`gorkie (dev)`) already seems to exist in practice, going by the bot's display name seen live in Slack throughout this project. Standard 12-factor-app separation (different `.env` per environment), not a Mastra-specific feature. Not yet implemented — no separate dev `DATABASE_URL`/`MASTRA_PROJECT_ID` currently exist as far as this checkout's `.env.example` documents.
